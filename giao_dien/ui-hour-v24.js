// V2.4 Giờ cá nhân — reference-only presentation. Reads /api/v2/gio-ca-nhan.
(function(){
  async function fetchHours(){return post('/api/v2/gio-ca-nhan',{profile:current()})}
  function card(r){const hours=r?.hours||[];return `<div class="card v24-hour-card"><small>GIỜ TRONG NGÀY</small><h3>${esc(r?.conclusion?.title||'Tham khảo cấu trúc giờ')}</h3><p>${esc(r?.plain_explanation||'')}</p><div class="v24-hour-note"><b>Trạng thái:</b> ${esc(r?.conclusion?.label||'Tham khảo cấu trúc')}</div><details><summary>Xem 12 khoảng giờ</summary><div class="v24-hour-grid">${hours.map(x=>`<div class="v24-hour-item"><b>${esc(x.time_range||'')}</b> · ${esc(x.chi_vi||x.chi||'')}<br><span>${esc(x.relation_label||'Không có quan hệ trực tiếp trong lớp hiện tại')}</span></div>`).join('')}</div></details><div class="soft-note">Chưa phải giờ tốt/xấu cá nhân. Không dùng giờ để cứu một ngày bị chặn.</div></div>`}
  function styles(){if(document.getElementById('v24-hour-style'))return;const s=document.createElement('style');s.id='v24-hour-style';s.textContent=`.v24-hour-card{border-left:5px solid #82918e}.v24-hour-card h3{margin:5px 0 8px}.v24-hour-note{margin:10px 0;font-size:13px}.v24-hour-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:12px}.v24-hour-item{border:1px solid var(--line,#d7e2de);border-radius:12px;padding:10px;font-size:13px;line-height:1.4}.v24-hour-item span{color:var(--muted,#64748b)}@media(max-width:640px){.v24-hour-grid{grid-template-columns:1fr}}`;document.head.appendChild(s)}
  window.openPersonalHoursV24=async function(){if(!needProfile())return;navTo('result');$('result-title').textContent='Giờ trong ngày';$('result-body').innerHTML='<div class="card">Đang đọc cấu trúc giờ…</div>';try{$('result-body').innerHTML=card(await fetchHours())}catch(e){$('result-body').innerHTML=`<div class="notice danger"><b>Chưa tải được mục Giờ trong ngày</b>${esc(e.message)}</div>`}}
  const previousQuestion=openQuestion;openQuestion=async function(kind){await previousQuestion(kind);if(kind!=='today')return;try{const body=$('result-body');if(body)body.insertAdjacentHTML('beforeend',card(await fetchHours()))}catch{}}
  styles();
})();
