// V2.3 Quan hệ — presentation layer only. Reads canonical /api/v2/quan-he.
(function(){
  function tone(r){const s=String(r?.conclusion?.state||'');if(s==='SUPPORT')return'good';if(s==='CAUTION')return'warn';return'neutral'}
  function actions(r){const yes=r?.recommended_actions||[],no=r?.cautions||[];return `<div class="v23-rel-actions"><section><h4>✓ Nên làm</h4>${yes.map(x=>`<p>${esc(x)}</p>`).join('')||'<p class="muted">Chưa có gợi ý riêng.</p>'}</section><section><h4>! Cần lưu ý</h4>${no.map(x=>`<p>${esc(x)}</p>`).join('')||'<p class="muted">Chưa có lưu ý riêng.</p>'}</section></div>`}
  function details(r){const rules=r?.rules||[],sources=r?.sources||[],evidence=r?.evidence||[];return `<details class="v23-rel-details"><summary>Vì sao app kết luận về quan hệ?</summary><p>${esc(r?.plain_explanation||'')}</p><div class="v23-rel-trace"><b>Căn cứ:</b> ${esc(r?.confidence_state||'Chưa đủ căn cứ')}</div>${evidence.length?`<div class="v23-rel-trace"><b>Dữ kiện dùng:</b> ${evidence.map(x=>esc(x.type||'evidence')).join(' · ')}</div>`:''}${rules.length?`<div class="v23-rel-trace"><b>Quy tắc:</b> ${rules.map(esc).join(' · ')}</div>`:''}${sources.length?`<div class="v23-rel-trace"><b>Nguồn:</b> ${sources.map(esc).join(' · ')}</div>`:''}<details class="v2-expert"><summary>Xem dữ liệu kỹ thuật</summary><pre>${esc(JSON.stringify(r?.technical||{},null,2))}</pre></details></details>`}
  function card(r,scope){const c=r?.conclusion||{};return `<div class="card v23-rel-card ${tone(r)}"><div class="v23-rel-head"><div><small>${scope==='month'?'QUAN HỆ THÁNG NÀY':'QUAN HỆ HÔM NAY'}</small><h3>${esc(c.title||c.label||'Chưa đủ căn cứ')}</h3></div><span class="v2-confidence ${tone(r)}">${esc(r?.confidence_state||'Chưa đủ căn cứ')}</span></div><p>${esc(r?.plain_explanation||'')}</p>${actions(r)}${details(r)}<div class="soft-note">V2.3 chỉ nói về tương tác và phối hợp thường ngày; không dự đoán yêu đương, hôn nhân, chia tay, ngoại tình hay đổ vỡ từ một tín hiệu đơn lẻ.</div></div>`}
  function styles(){if(document.getElementById('v23-rel-style'))return;const s=document.createElement('style');s.id='v23-rel-style';s.textContent=`.v23-rel-card{border-left:5px solid #82918e}.v23-rel-card.good{border-left-color:#16856f}.v23-rel-card.warn{border-left-color:#c38b22}.v23-rel-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.v23-rel-head h3{margin:5px 0 7px;line-height:1.35}.v23-rel-head small{font-size:12px;font-weight:800;letter-spacing:.04em;color:var(--muted,#64748b)}.v23-rel-card>p{line-height:1.5}.v23-rel-actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:14px 0}.v23-rel-actions section{border:1px solid var(--line,#d7e2de);border-radius:14px;padding:12px}.v23-rel-actions h4{margin:0 0 8px}.v23-rel-actions p{margin:6px 0;line-height:1.45}.v23-rel-details{margin-top:12px;border-top:1px solid var(--line,#d7e2de);padding-top:12px}.v23-rel-details>summary{cursor:pointer;font-weight:800}.v23-rel-trace{margin:8px 0;font-size:13px}.v23-rel-home{margin-top:12px}.v23-rel-home button{width:100%;text-align:left;border:0;background:transparent;padding:0;color:inherit}.v23-rel-home h3{margin:4px 0 6px}.v23-rel-home p{margin:0;line-height:1.45}@media(max-width:640px){.v23-rel-actions{grid-template-columns:1fr}.v23-rel-head{display:block}.v23-rel-head .v2-confidence{display:inline-block;margin-top:4px}}`;document.head.appendChild(s)}
  async function fetchRelationship(scope='day'){return post('/api/v2/quan-he',{profile:current(),scope})}
  async function refreshHomeRelationship(){if(!currentProfile)return;const prev=$('home-finance-summary-v22')||$('home-work-summary-v21')||$('home-today-summary');if(!prev)return;let box=$('home-relationship-summary-v23');if(!box){box=document.createElement('div');box.id='home-relationship-summary-v23';box.className='home-today-card v23-rel-home';prev.insertAdjacentElement('afterend',box)}box.innerHTML='<div class="loading-line">Đang đọc riêng lĩnh vực quan hệ…</div>';try{const r=await fetchRelationship('day'),c=r.conclusion||{};box.innerHTML=`<button onclick="openRelationshipDomain('day')"><small>QUAN HỆ HÔM NAY · V2.3</small><h3>${esc(c.title||c.label||'Chưa đủ căn cứ')}</h3><p>${esc(r.plain_explanation||'')}</p><div class="v2-meta"><span class="v2-confidence ${tone(r)}">${esc(r.confidence_state||'Chưa đủ căn cứ')}</span></div><span class="v2-home-more">Xem nên làm gì và vì sao ›</span></button>`}catch(e){box.innerHTML=`<div class="notice danger"><b>Chưa tải được Quan hệ V2.3</b>${esc(e.message)}</div>`}}
  window.openRelationshipDomain=async function(scope='day'){if(!needProfile())return;navTo('result');$('result-title').textContent=scope==='month'?'Quan hệ tháng này':'Quan hệ hôm nay';$('result-body').innerHTML='<div class="card">Đang đọc riêng lĩnh vực quan hệ…</div>';try{const r=await fetchRelationship(scope);$('result-body').innerHTML=card(r,scope)}catch(e){$('result-body').innerHTML=`<div class="notice danger"><b>Chưa lấy được kết quả Quan hệ V2.3</b>${esc(e.message)}</div>`}}
  const previousHome=loadHomeDashboard;loadHomeDashboard=async function(){await previousHome();await refreshHomeRelationship()};
  const previousQuestion=openQuestion;openQuestion=async function(kind){await previousQuestion(kind);if(kind!=='today'&&kind!=='month')return;try{const scope=kind==='month'?'month':'day',r=await fetchRelationship(scope),body=$('result-body');if(body)body.insertAdjacentHTML('beforeend',card(r,scope))}catch{}}
  styles();window.addEventListener('load',()=>setTimeout(()=>{try{refreshHomeRelationship()}catch{}},1800));
})();
