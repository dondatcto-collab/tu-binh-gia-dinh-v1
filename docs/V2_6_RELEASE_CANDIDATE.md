# V2.6 Release Candidate

Phạm vi release candidate:
- hợp nhất bootstrap UI thành một điểm tải duy nhất;
- bỏ tải chéo Work → Finance → Relationship → Hour;
- chuẩn hóa nhãn phiên bản hiển thị cho người dùng;
- cập nhật PWA cache sang V2.6;
- không thay engine, API quyết định, Hiệp Kỷ, ranking hay logic giờ cá nhân.

Cổng phát hành:
GitHub regression PASS → một Vercel preview cuối → PR PASS → merge main → production smoke.
