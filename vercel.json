{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "git": {
    "deploymentEnabled": {
      "v3-0e*": false
    }
  },
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then exit 1; elif printf '%s' \"$VERCEL_GIT_COMMIT_MESSAGE\" | grep -q '\\[vercel-preview\\]'; then exit 1; else exit 0; fi"
}
